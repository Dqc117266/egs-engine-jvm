/*
 * Hand-written domain Koin bindings (custom use cases). Safe from API sync overwrites.
 */
package org.mifos.feature.profile.di

import org.koin.dsl.module

internal val domainModule = module {
}
