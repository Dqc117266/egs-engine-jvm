<#-- Domain prefs slice interface -->
package ${domainRepositoryPackage}

internal interface ${modulePascal}PrefsRepository {
    // egs-gen:prefs-repository-begin
    // egs-gen:prefs-repository-end
}
