extends "${userExtendsPath}"
class_name ${pascal}
## ${pascal} — user stub. Created once by `egs add room ${name}`.
## Edit freely; never overwritten. Add exits, spawn points, etc. here.


func _ready() -> void:
	super._ready()
