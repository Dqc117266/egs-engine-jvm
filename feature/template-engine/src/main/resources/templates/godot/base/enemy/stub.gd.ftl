extends "${userExtendsPath}"
class_name ${pascal}
## ${pascal} — user stub. Created once by `egs add enemy ${name}`.
##
## Edit freely: this file is never overwritten. Override _ready() to tune
## stats, add state-machine behaviour, wire signals, etc.


func _ready() -> void:
	super._ready()
