extends "${userExtendsPath}"
class_name ${pascal}
## ${pascal} — user stub. Created once by `egs add skill ${name}`.
## Edit freely; never overwritten. Override activate() for the runtime effect.


func activate(_context: Dictionary) -> void:
	super.activate(_context)
